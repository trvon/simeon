build/
build-*/
benchmarks/results/
fixtures/
.claude/
*.jsonl
*.profraw
*.profdata

# Local research / engineering helpers are intentionally not part of the
# open-source surface. Keep the one contributor-facing formatter script.
benchmarks/*.py
scripts/*.py
scripts/*.sh
!scripts/format-code.sh
